# مراجع تجهيز دمج الإعلانات

## ExoClick — فيديو داخل المشغل

- المصدر: <https://www.exoclick.com/boost-your-website-revenue-with-video-ads/>
- يوضح المصدر أن **In-Stream Video** يعمل داخل مشغل HTML5 ويدعم مشغلات الفيديو القياسية، مع خيارات **Pre-roll وMid-roll وPost-roll**.
- للاستخدام المقترح في MIX GOLD: إنشاء منطقة In-Stream / Pre-roll فقط، مع زر تخطٍ ظاهر بعد 5 ثوانٍ، وعدم إضافة Mid-roll.
- توصي ExoClick بتحديد تكرار مناسب؛ وتذكر أن نافذة قدرها 10 دقائق تحقق توازنًا أفضل بين العائد وتجربة المستخدم.
- لا يحتاج التركيب إلى بيانات دخول؛ البيانات المطلوبة لاحقًا هي رابط VAST أو Zone ID/Ad Tag الخاص بالمنطقة الفعلية فقط.

## اختبار VAST في ExoClick

- المصدر: <https://docs.exoclick.com/tutorials/tutorials/publishers-tutorials/how-implement-in-stream-part3>
- توفر ExoClick روابط VAST تجريبية للتحقق من توافق المشغل، ثم يجب استبدالها بالـZone الفعلي. ولا يعني رد VAST الفارغ أن الدمج خاطئ بالضرورة.
- لا يُستخدم أي Zone تجريبي في النشر النهائي.

## Adsterra — Native Banner وSocial Bar

- المصادر: <https://adsterra.com/social-bar-ad/> و<https://adsterra.com/native-banner-ad/>.
- Social Bar صيغة تفاعلية تعمل على الجوال وسطح المكتب ولا تتطلب موافقة Web Push؛ يمكن أن تظهر كـ In-Page Push أو إشعار/ودجت أو Interstitial بحسب الإعداد.
- لأجل تجربة MIX GOLD: يُنشأ Zone منفصل لـSocial Bar، ويُضبط على ظهور محدود مرة واحدة لكل زيارة، مع إيقاف صيغ Popunder وInterstitial والأنواع التي تغطي المحتوى.
- للمواضع الثابتة، يُنشأ Zone مستقل لوحدة Native Banner / Native Ads. لا يُعاد استخدام Zone Social Bar في مكان Native Banner.
- التركيب اللاحق لا يحتاج بيانات دخول: يكفي Script/Ad Tag الكامل لكل Zone، مع اسم الوحدة لتمييزها (Native أو Social Bar)، ومعلومة أن الموقع تمّت الموافقة عليه داخل الحساب.

## خريطة الدمج المعتمدة في MIX GOLD

| الوحدة | الموضع الدقيق | ما لن يتم وضعه فيها | قاعدة الظهور |
|---|---|---|---|
| ExoClick In-stream / Pre-roll | صفحة تفاصيل **الفيديو فقط** داخل `#post-detail`، في طبقة تشغيل مُدارة بجوار `#post-player-iframe` | الصفحة الرئيسية، القوائم، تفاصيل الصور، المانجا والمانهوا، قارئ المانجا | يبدأ قبل مشغل الفيديو الفعلي، ولا يُعاد تحميله عند فتح قسم آخر |
| Adsterra Native Banner | أسفل تفاصيل المنشور، بعد المحتوى التعريفي وقبل «Related / Suggested» | داخل إطار مشغل الفيديو، فوق أزرار المشغل أو المشاركة، داخل صفحة 404 | وحدة واحدة ثابتة لكل صفحة تفاصيل مؤهلة |
| Adsterra Native Banner | داخل معرض الصور متعدد العناصر بواسطة حاويات `data-ad-slot` الموجودة أصلًا | أول صورة من المعرض أو أثناء التكبير، وقارئ المانجا | بعد كل `AD_FREQUENCY` عناصر؛ سيُضبط المتغير بين 6 و8 بعد اختيار المقاس المناسب للجوال |
| Adsterra Social Bar | في مستوى الموقع مرة واحدة عند أول تحميل زيارة المتصفح | قارئ المانجا، ولا يُكرر عند تغيير القسم أو فتح رابط منفصل في نفس الزيارة | حاجز `sessionStorage` باسم ثابت؛ لا يحقن السكربت إذا كانت الزيارة سجلت ظهوره |

## ما سيُستبدل من الكود الموجود

يوجد نموذج Adsterra قديم في `client/index.html` عبر الدوال `buildAdSlotHtml()` و`populateAdSlots()`، لكنه يعتمد مفتاحًا/مقاسًا عامًا ولا يكفي للنشر. عند وصول الكود الحقيقي ستُستبدل هذه الطبقة فقط بكود **Native Banner** الذي تولّده Adsterra، مع الإبقاء على حاويات الصور الموجودة. وستُضاف طبقة مستقلة لـSocial Bar مع منع التكرار خلال الجلسة.

لن يُضاف أي كود إعلاني إلى `#manga-reader-view` أو داخل صور الصفحات أو آلية السحب الأفقي. كما لن يُغيَّر تصميم الموقع أو وظائفه الأساسية؛ التغيير مقتصر على حاويات إعلان واضحة في المواضع المذكورة أعلاه.

## المطلوب إرساله بعد إنشاء الحسابات

| الشبكة | ما يجب نسخه وإرساله هنا | مثال لما لا يجب إرساله |
|---|---|---|
| ExoClick | **VAST / In-stream Ad Tag الكامل** أو **Zone ID** الخاص بوحدة Video Pre-roll، مع تأكيد أن الوحدة مخصّصة للموقع `mix-gold-jet.vercel.app` | البريد، كلمة المرور، رمز 2FA، مفاتيح API السرية، بيانات دفع |
| Adsterra — Native | كود **Native Banner / Native Ads** الكامل من صفحة Get Code، مع كتابة `Native` فوقه | بيانات تسجيل الدخول أو لقطات شاشة من لوحة الحساب |
| Adsterra — Social Bar | كود **Social Bar** الكامل من صفحة Get Code، مع كتابة `Social Bar` فوقه | أي رمز تحقق أو Cookie أو بيانات حساب |

> إذا كانت الشبكة تعطي عدة خيارات للكود، يجب اختيار الكود المخصص لموقع الويب وليس للتطبيقات أو Smartlink. لا حاجة مطلقًا لمشاركة بيانات الدخول؛ الوسم الإعلاني العام أو معرّف المنطقة وحده هو المطلوب للتركيب.

## تسلسل التنفيذ بعد وصول الأكواد

1. أتحقق من نوع كل وسم ومن عدم احتوائه على صيغ غير مطلوبة، ثم أستبدل النموذج القديم دون كشف أو تخزين بيانات حساب.
2. أضيف ExoClick فقط لمسار الفيديو، وNative Banner لصفحة التفاصيل والمعرض، ثم Social Bar مرة واحدة للجلسة مع استثناء قارئ المانجا.
3. أزامن `client/index.html` و`client/public/mix-gold.html`، وأضيف اختبارات حماية تؤكد عدم وجود أي إدراج إعلاني داخل `#manga-reader-view` وعدم تكرار Social Bar.
4. أشغّل الاختبارات والبناء، وأرفع النسخة إلى GitHub ليُحدّث Vercel الرابط الرسمي `https://mix-gold-jet.vercel.app/`، ثم أرسل لك ملف HTML المحدّث مباشرة.

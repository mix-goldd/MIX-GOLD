# تحقق صفحة تفاصيل الفيديو — 16 أغسطس 2026

- تم فتح المنشور المنشور على Vercel: `https://mix-gold-jet.vercel.app/post/1d9vzr1`.
- ظهر قسم **Categories** مع التصنيف المتوفر `Shonen` فقط.
- لم يظهر قسم الاستوديو أو السلسلة، ولم يظهر عنوان قسم النجوم، لأن هذه البيانات غير موجودة لهذا المنشور.
- نجح `pnpm test` (9 اختبارات) و`pnpm build` قبل التحقق المباشر.

## صفحة 404 للمنشورات غير المتوفرة — 16 أغسطس 2026

- تم التحقق محليًا بصريًا على رابط `/post/not-available` بعرض هاتف؛ ظهرت بطاقة 404، والنص العربي والإنجليزي، وزر العودة إلى الصفحة الرئيسية بصورة متجاوبة.
- أعاد فحص المصدر المنشور من `https://mix-gold-jet.vercel.app/post/not-available` الواجهة التي تحتوي العنصر `post-not-found`، مما يؤكد وصول نسخة 404 إلى Vercel.
- لم يكتمل العرض المرئي الثاني على المتصفح الخارجي بسبب انتقال جلسة المتصفح إلى `about:blank` بعد الفحص؛ لذلك استند تحقق النشر إلى محتوى المصدر، بينما تم التحقق المرئي من نسخة التطوير.

## تشخيص ملخص القصة للمنشور Bleach — 17 أغسطس 2026

- تحتوي قاعدة بيانات Supabase على ملخص القصة الصحيح للمنشور `Bleach: Sennen Kessen-hen - Kashin-tan الحلقة 1` في عمود `description`، كما تحتوي على التصنيفات `Shonen` و`Action` و`Adventure` و`Supernatural` في عمود `categories`.
- ظهر النص غير المرتبط بكرسي متحرك لأن عنصر `#desc-text` في واجهة الموقع يحتوي نصًا تجريبيًا ثابتًا، ولأن محول منشورات الفيديو لم يكن ينقل `p.description` إلى بيانات تفاصيل المنشور.
- لذلك فالمشكلة في واجهة الموقع، وليست في إدخال لوحة التحكم أو في حفظ بيانات Supabase.

## فصل بيانات منشورات Bleach — 17 أغسطس 2026

- لكل حلقة Bleach سجل مستقل ومعرّف مستقل في جدول `posts`.
- يُحفظ ملخص كل حلقة منفصلًا في `post_details`، وتُحفظ التصنيفات في سجلات منفصلة داخل `post_categories` مرتبطة بمعرّف المنشور نفسه.
- لا توجد نجوم مرتبطة بهذين السجلين حاليًا، لذلك تبقى قائمة النجوم مخفية لهما كما هو مطلوب.
- أظهر فحص القراءة النهائي أن الحلقتين تحتفظان بملخصين عربيين مختلفين وروابطي مشاهدة وتنزيل Vidmoly منفصلين، رغم اشتراكهما في التصنيفات نفسها؛ لذلك لا تختلط بيانات منشور مع آخر.

## اختبار زر الرجوع — 17 أغسطس 2026

- تم فتح المنشور `https://mix-gold-jet.vercel.app/post/6t1nx1` من الصفحة الرئيسية المنشورة.
- لم تُحرّك محاكاة مفتاح الرجوع في بيئة الاختبار عنوان الرابط؛ لذلك لم تُستخدم وحدها للحكم على سجل الموقع.
- أُجري اختبار سجل متصفح فعلي واحد من حالة المنشور نفسها: كانت الحالة قبل الرجوع `{mixGold: true, slug: "6t1nx1", view: "post"}`، وبعد استدعاء رجوع واحد صدر حدث `popstate` وانتقل الرابط مباشرة إلى `https://mix-gold-jet.vercel.app/` بحالة `{mixGold: true, page: "home", view: "page"}`، من دون صفحة أو مشغل وسيط.

## تشخيص بقاء المشغل الأسود عند الرجوع — 17 أغسطس 2026

- فحصت النسخة المنشورة على الرابط الرئيسي: كانت حالة السجل للصفحة الرئيسية `{mixGold: true, page: "home", view: "page"}`، وكان `#post-detail` بلا الفئة `active` ومعروضًا بقيمة `display: none`، ومصدر `#post-player-iframe` هو `about:blank`.
- سيجعل الإصلاح مسار `popstate` يزيل صراحةً حالة تفاصيل المنشور قبل حلّ الرابط التالي، بدل الاعتماد على انتقال الصفحة اللاحق فقط؛ وبذلك يكون الإخفاء والتنظيف مضمونين حتى إذا تغير مسار الرجوع أو تعذر حلّ الصفحة التالية.
- فُتحت نسخة التطوير الجديدة وأُغلقت نافذة الشروط بنجاح، وظهرت بطاقتا منشور Bleach في الصفحة الرئيسية؛ أصبحت البيئة جاهزة لاختبار فتح منشور والرجوع منه.
- فُتح منشور الحلقة الثانية من البطاقة بنجاح على المسار المحلي `/post/6t1nx1`. لم يغير اختصار `Alt+Left` في بيئة الاختبار عنوان الرابط، لذلك لن يُستخدم وحده للحكم على رجوع الهاتف؛ سيُختبر انتقال السجل المباشر بدلًا منه.
- لم يغيّر استدعاء `history.back()` في جلسة المتصفح الآلية عنوان الرابط، وهي نفس محدودية بيئة الاختبار التي ظهرت سابقًا. لذلك سيُفحص منطق حدث `popstate` نفسه عبر محاكاة حالة رجوع إلى الصفحة الرئيسية، مع قراءة حالة تفاصيل المنشور ومصدر الإطار بعدها.
- نجح اختبار معالجة `popstate` في نسخة التطوير: بعد محاكاة رجوع إلى `/` أصبحت تفاصيل المنشور غير نشطة (`detailActive: false`) وبالقيمة `display: none`، وأصبح مصدر الإطار `about:blank`، وعادت الصفحة الرئيسية إلى الحالة النشطة. أكّد الفحص البصري ظهور بطاقات الصفحة الرئيسية بدل مساحة المشغل.
- رُفع الإصلاح إلى GitHub في Commit `de1c3ee`، وأكّد فحص الموقع الرئيسي المنشور على Vercel وجود تنظيف المشغل ومعالجة إزالة الحالة النشطة من تفاصيل المنشور ضمن ملف الصفحة المنشور.

## تبديل أيقونتي Comics — 17 أغسطس 2026

- استُبدل ترتيب أدوات صف **Comics** فقط؛ أصبح سهم فتح القائمة قبل أيقونة القفل، مع إبقاء الأداتين كمجموعة واحدة في نهاية الصف حتى لا يتغير موضعهما أو وظيفة القفل.
- نجح `pnpm test` بـ21 اختبارًا و`pnpm build`، ثم أُخذت لقطة تحقق محلية بعرض هاتف 375×812 بعد التعديل.
- رُفع التعديل إلى GitHub في Commit `f378c2f`، وأكّد فحص الموقع الرئيسي المنشور على Vercel وجود الصنف `comics-dropdown-controls`، أي وصول ترتيب الأيقونتين الجديد إلى النشر الرسمي.

## استبدال شعار MIX GOLD — 17 أغسطس 2026

- نُزّل الشعار الذي أرسله المستخدم وحُفظ كأصل ثابت ثم رُفع إلى مسار النشر `/manus-storage/mix-gold-logo_310bd072.png`.
- أظهر فحص نسخة التطوير على هاتف 375×812 الشعار الجديد في مركز الترويسة بالمقاس الحالي دون تغيير موضع زر القائمة أو البحث.
- نجح `pnpm test` بـ22 اختبارًا و`pnpm build` بعد الاستبدال.
- رُفع التعديل إلى GitHub في Commit `357fab9`، وأكّد فحص المصدر الرسمي على Vercel وجود المسار `/manus-storage/mix-gold-logo_310bd072.png` في شعار الترويسة.

## معاينات روابط المنشورات في وسائل التواصل — 17 أغسطس 2026

- أُضيفت وظيفة Vercel في `api/post-preview.mjs` لمسار كل منشور. تستعلم الوظيفة عن سجل المنشور وصورته من Supabase، ثم تعيد واجهة MIX GOLD نفسها بعد حقن عنوان الصفحة ووصفها ورابطها وصورتها في بيانات Open Graph وTwitter Card.
- جرى تقييد اختيار الصورة المصغرة بالغلاف والصور المرتبطة بالمنشور فقط، فلا تستخدم روابط الفيديو كصورة مشاركة. تُستخدم صورة شعار MIX GOLD فقط كبديل عند عدم وجود صورة منشور.
- نجح `pnpm test` بـ23 اختبارًا و`pnpm build`، كما نجح فحص صياغة وظيفة Vercel بـ`node --check api/post-preview.mjs`.
- بعد رفع Commit `900960d`، أُعيد فحص الرابط `https://mix-gold-jet.vercel.app/post/6t1nx1` بمحاكاة قارئ مشاركة. أعاد العنوان والوصف والصورة الصحيحة للمنشور، بما في ذلك `og:image` لصورة الغلاف و`twitter:card=summary_large_image`، مع بقاء الرابط نفسه في `og:url`.

## تحجيم عنوان صفحة المنشور حسب طوله — 17 أغسطس 2026

- أُضيفت دالة `fitHeaderPageTitle()` التي تقيس المساحة الحقيقية المتبقية بين زر القائمة وزر البحث، ثم تضبط حجم العنوان تدريجيًا؛ يظل العنوان القصير بالحجم المعتاد، بينما يصغر العنوان الطويل حتى لا يُقص.
- نجح `pnpm test` بـ24 اختبارًا و`pnpm build`. وأظهر فحص هاتف 375×812 عنوان منشور طويلًا كاملًا في الترويسة دون علامة حذف أو تداخل مع أزرارها.
- رُفع التعديل إلى GitHub في Commit `f4c6719`، وأكّد فحص المصدر المنشور على Vercel وجود الاستدعاء `fitHeaderPageTitle()` ضمن الصفحة الرئيسية الرسمية.

## صورة معاينة مشاركة 16:9 — 17 أغسطس 2026

- تُحوَّل صورة الغلاف المستخدمة في معاينة المشاركة إلى صورة عريضة ثابتة بدقة `1200×675` ونسبة `16:9` مع قص متوازن يحافظ على امتلاء الإطار، ثم يُستخدم هذا المسار في `og:image` و`twitter:image`.
- أُضيفت بيانات `og:image:width` و`og:image:height` و`og:image:type` كي تتعرف منصات التواصل إلى أبعاد الصورة الصحيحة قبل إظهارها.
- نجح `pnpm test` بـ24 اختبارًا و`pnpm build`، كما أكد اختبار الصورة الناتجة محليًا أنها JPEG بدقة `1200×675`.
- بعد رفع Commit `7d0a441`، أُعيد فحص رابط منشور حقيقي بمحاكاة قارئ مشاركة. احتوى على `twitter:card=summary_large_image` وعلى `og:image` العريض المعرّف بالأبعاد `1200×675`، وعادت الصورة عبر المسار المنشور بنسبة 16:9 (738×415 بعد تحسين التسليم).

## تحقق مباشر من معاينة فيسبوك — 17 أغسطس 2026

- أُعيد جلب الرابط `https://mix-gold-jet.vercel.app/post/6t1nx1` بهوية قارئ فيسبوك. أظهر المصدر المنشور `og:image` و`twitter:image` بالرابط نفسه، و`og:image:width=1200` و`og:image:height=675` و`twitter:card=summary_large_image`.
- حُمّلت الصورة التي يقرأها فيسبوك من رابط `og:image` مباشرة؛ وهي ملف JPEG فعلي بأبعاد `1200×675`، أي نسبة **16:9** بالضبط، من دون حواف سوداء أو انضغاط أو تمديد.
- تُظهر لقطة المستخدم أن تطبيق فيسبوك على الهاتف يعرض مربع المعاينة بقص مركزي عمودي/مربع داخل واجهة كتابة المنشور. هذا **قصّ عرض من فيسبوك داخل التطبيق** وليس تغييرًا في صورة الموقع أو أبعادها؛ الجزء الظاهر يطابق منتصف الصورة العريضة نفسها.

## ضبط عنوان ترويسة المنشور على الهاتف — 17 أغسطس 2026

- عولج سبب بقاء العنوان كبيرًا: كان القياس قد يحدث قبل اكتمال تخطيط الترويسة، كما كان قياس العرض الطبيعي يتأثر بالحد السابق للعنصر.
- أصبح القياس يتكرر في إطارَي رسم بعد إظهار العنوان، ويقيس عرضه الطبيعي بلا حد، ثم يخصص مساحة آمنة أكبر حول زري القائمة والبحث. يبدأ العنوان على الهاتف من حجم محافظ `14.4px`، ويصغر تدريجيًا عند الحاجة إلى حد `7.5px` بدل القص أو التداخل.
- نجح `pnpm test` بـ24 اختبارًا و`pnpm build`. كما أظهر التحقق من رابط Vercel المنشور `https://mix-gold-jet.vercel.app/post/6t1nx1` العنوان كاملًا في مركز الترويسة بين الأزرار دون علامة حذف.
- رُفع التحديث إلى GitHub في Commit `5503607` ليصل تلقائيًا إلى Vercel.

## إعادة ضبط عنوان الهاتف للحجم المقروء — 17 أغسطس 2026

- أظهر اختبار حقيقي على شاشة `360×780` أن التصغير السابق إلى `7.5px` جعل العنوان صغيرًا أكثر من اللازم، رغم منع التداخل.
- ثُبّت حجم العنوان على الهاتف عند `16px` ليبقى واضحًا ومناسبًا، وزيدت المساحة المتاحة بين الأزرار بتقليل هامش الأمان إلى `20px`. تستخدم العناوين الطويلة علامة الحذف الطبيعية بدل تصغيرها إلى حجم غير مقروء.
- نجح `pnpm test` بـ24 اختبارًا و`pnpm build`. أظهر اختبار الهاتف الجديد عنوانًا واضحًا مثل `Bleach: Sennen Kessen-hen - Ka…` بين زر القائمة والبحث دون تداخل.
- رُفع الإصلاح إلى GitHub في Commit `9325eb9`، وأعاد Vercel صفحة المنشور المنشورة بحالة HTTP `200` بعد التحديث.

## إظهار عنوان المنشور ورقم الحلقة كاملين على الهاتف — 17 أغسطس 2026

- بقياس عرض الهاتف `360×780` تبيّن أن عنوان الحلقة المختلط بين العربية والإنجليزية يحتاج سطرين مقروءين بدل اختصاره بعلامة الحذف أو خفض حجم الخط إلى قيمة غير مناسبة.
- أصبح العنوان الطويل يلتف تلقائيًا على سطرين بحجم يبدأ من `15px` ويصغر تدريجيًا إلى حد `13px` عند الحاجة. استُخدم فاصل صريح بين اسم العمل ورقم الحلقة، وعُزل اتجاه عبارة الحلقة كي تبقى مثل `الحلقة 2` مترابطة في الترويسة.
- نجح `pnpm test` بـ24 اختبارًا و`pnpm build`، والتحقق البصري بعرض `360px` أظهر العنوان في سطرين بين زري القائمة والبحث دون تداخل أو علامة حذف.
- رُفع الإصلاح إلى GitHub في Commit `8af4fb1`، ثم أعادت صفحة Vercel المنشورة `https://mix-gold-jet.vercel.app/post/6t1nx1` حالة HTTP `200` بعد وصول التحديث.

## فحص مصدر Vercel الرسمي — 17 أغسطس 2026

- الرابط الرسمي `https://mix-gold-jet.vercel.app/post/6t1nx1` يعمل ويعيد عنوان الصفحة الكامل: `Bleach: Sennen Kessen-hen - Kashin-tan الحلقة 2 | MIX GOLD`.
- يعتمد هذا الفحص رابط Vercel وحده لنتيجة النشر؛ نطاق Manus للمعاينة الداخلية فقط ولا يمثل معيار القبول.

## تشخيص فرق Vercel وGitHub — 17 أغسطس 2026

- نسخة `index.html` في فرع GitHub الرئيسي تطابق النسخة المحلية المحدثة وتحتوي قاعدة الهاتف الجديدة التي تسمح بعنوان الترويسة بسطرين كاملين.
- ملف HTML الذي يرسله `mix-gold-jet.vercel.app` ما زال يحمل قاعدة الترويسة السابقة أحادية السطر؛ لذلك يظهر العنوان مختصرًا في لقطة المستخدم.
- لا يتاح الوصول الحالي إلى لوحة مشروع Vercel: تتطلب صفحة المشروع تسجيل الدخول، وموصل Vercel لا يعرض مشروعًا في الفريق. يلزم فتح مشروع Vercel الصحيح وإعادة نشر آخر Commit من GitHub أو إعادة ربطه بالفرع `main`.

## حالة الوصول بعد تسجيل المستخدم دخوله إلى Vercel — 17 أغسطس 2026

- سجّل المستخدم دخوله بنجاح إلى فريق Vercel الصحيح على جهازه، وهو ظاهر باسم `mixgoldd100-6400` في لوحة Vercel على الهاتف.
- جلسة المتصفح الحالية منفصلة عن جلسة الهاتف؛ فتح رابط المشروع في هذه الجلسة ما زال يعيد إلى صفحة تسجيل دخول Vercel، وكذلك لم تتوفر جلسة GitHub محفوظة لمسار الدخول البديل.
- لا يمكن تنفيذ إعادة النشر على نطاق Vercel الرسمي من هذه الجلسة حتى تصل مصادقة صالحة إلى لوحة مشروع Vercel أو يتاح وصول موصل Vercel إلى المشروع.

## اختبار تنقل القائمة من صفحة منشور — 17 أغسطس 2026

- فُتح المنشور الرسمي `https://mix-gold-jet.vercel.app/post/6t1nx1`، ثم فُتحت القائمة الجانبية واختيرت صفحة **Settings** منها.
- كشف الاختبار المباشر أن الدالة `openStandalonePage` كانت تستدعي `getPageName(page)`، وهي دالة غير معرّفة؛ لذلك كان النقر يُلغى قبل تنفيذ الانتقال.
- استُبدل الاستدعاء بـ`getPageTitle(page)` في ملف الصفحة وفي النسخة الاحتياطية، وأضيف اختبار يمنع عودة الاستدعاء غير المعرّف. اجتازت اختبارات المشروع 30 اختبارًا ونجح بناء الإنتاج.
- رُفع الإصلاح إلى GitHub في Commit `9d12929`، وأكد فحص المصدر الرسمي وصول الاستدعاء المصحح إلى `https://mix-gold-jet.vercel.app/`.
- أعيد تنفيذ المسار نفسه بعد النشر: من صفحة المنشور فُتحت القائمة الجانبية ثم نُقر **Settings**، وانتقل المتصفح فعليًا إلى `https://mix-gold-jet-settings.vercel.app/`.

## شاشة Google السوداء على Android — 17 أغسطس 2026

- أكّد الفحص أن مسار Supabase التقليدي يصل إلى شاشة Google، لكن إعادة التوجيه الكاملة هي التي تتحول إلى شاشة سوداء على جهاز Android كما ظهر في لقطة المستخدم.
- أضيف بديل Google Identity Services مع FedCM: يطلب رمز تعريف من الواجهة المدمجة للمتصفح ثم يرسله إلى Supabase، مع بقاء المسار التقليدي بديلًا عندما لا تكون الخدمة متاحة.
- اجتاز التعديل 31 اختبارًا وبناء الإنتاج، ووصل إلى فرع GitHub `main` في Commit `2c7d321`.
- فحص نطاق Vercel الرسمي بعد أكثر من مهلة نشر ما زال يعرض نسخة أقدم بلا `use_fedcm_for_prompt` أو `signInWithIdToken`، كما تعرض صفحة الدخول زر Google السابق؛ لذلك لم يُختبر البديل الحي بعد.
- سبب التأخر خارج ملفات الموقع: موصل Vercel في هذه الجلسة لا يجد أي مشروع ضمن الفريق، وتسجيل دخول لوحة Vercel في المتصفح غير متاح، بينما يؤكد GitHub أن آخر Commit موجود على `main`.

## فقدان الحساب عند فتح Saved — 18 أغسطس 2026

- تؤكد لقطة الهاتف الأولى أن صورة حساب المستخدم تظهر في القائمة الجانبية على الموقع الرئيسي `mix-gold-jet.vercel.app`.
- تؤكد اللقطة الثانية أن صفحة `mix-gold-jet-saved.vercel.app` تعرض صورة الحساب الافتراضية بعد الانتقال، رغم بقاء زر Saved محددًا؛ لذلك فالمشكلة في استعادة جلسة الحساب بين نطاقي Vercel المنفصلين وليست في بيانات الملف الشخصي.
- نُقلت استعادة الجلسة إلى بداية `init()` قبل تحميل المحتوى وبناء القائمة، ثم صار `checkUserSession({ skipTransfer: true })` يستخرج بيانات الحساب والصورة قبل أول رسم مستقر للواجهة.
- اجتازت 33 اختبارًا وبناء الإنتاج، ووُلدت 13 صفحة مستقلة بالمنطق نفسه. بعد Commit `3630f75`، أكد فحص المصدر الحي لـ`https://mix-gold-jet-saved.vercel.app/` وجود الاستدعاء `await checkUserSession({ skipTransfer: true });`.

## تعطل تنقل القائمة في الصفحات المستقلة — 18 أغسطس 2026

- أظهر الفحص الحي لصفحة `https://mix-gold-jet-notifications.vercel.app/` أن روابط القائمة ما زالت تعتمد على معالجات `onclick` مباشرة، مثل `event.preventDefault(); openSavedStandalone()` و`openStandalonePage('settings')`.
- سيُستبدل ذلك بمعالج مفوّض واحد على القائمة، مع الإبقاء على روابط `href` نفسها كبديل للمتصفح، وفحص القفل ونقل جلسة الحساب قبل تغيير النطاق.
- عند تنفيذ ضغط حي على رابط Saved من صفحة Notifications بقي الرابط على صفحة Notifications؛ إذًا لا يكفي وجود `href` في العلامة طالما يلغي المعالج المباشر الحدث قبل اكتمال التحويل.

## قبول Agreement عبر الصفحات المستقلة — 18 أغسطس 2026

- تم التحقق حيًا من فتح صفحة Watch History بالرابط الذي يتضمن الجزء `#mixgold_agreement=<timestamp>` بعد قبول Agreement في صفحة أخرى.
- قرأت الصفحة قيمة الجزء المنقول عند التحميل، فحفظت حالة القبول محليًا قبل فحص طبقة Agreement؛ لذلك بقيت الطبقة مخفية ولم يُطلب قبول الاتفاقية مرة ثانية.
- يؤكد ذلك أن قبول Agreement ينتقل من الموقع الرئيسي إلى Watch History، مع بقاء رابط الصفحة الأساسي بلا أي معاملات زائدة قبل `#`.
